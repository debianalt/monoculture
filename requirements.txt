# Numerically-critical packages are pinned exactly: the MCA SVD (prince) and
# Ward linkage / silhouette (scipy, scikit-learn) are version-sensitive.
# Under these pins, regenerating the organisational partition from raw matches
# the frozen acm/data/org_cluster_assignments_v2.csv at ~99.4% of records, and
# the headline spatial-uniformity claims (17/17 departments, 180/180 census
# radios SAS-dominant under Milei) are invariant. See 04_run_acm.py and
# 00b_phase0_position_checks.py.
prince==0.16.5
scikit-learn==1.7.2
scipy==1.16.3
numpy==2.3.5
pandas==2.3.3
# Presentation only (do not affect the partition):
matplotlib>=3.7
adjustText>=0.8
Pillow>=9.0
