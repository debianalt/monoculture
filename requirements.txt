# The paper's analysis (code/) needs only numpy, pandas and scipy. Versions are
# pinned because the bootstrap and permutation nulls are seeded: under these
# pins every table in tables/ regenerates bit-identically.
numpy==2.3.5
pandas==2.3.3
scipy==1.16.3            # exact Poisson intervals, via chi2
# Optional: only 11_study_area_map.py and the dept-env table touch a database.
# sqlalchemy>=2.0
#
# Figures only (affect no reported quantity):
matplotlib>=3.7
Pillow>=9.0
#
# archive/ ONLY — the discarded correspondence-analysis and sequence pipeline.
# Nothing in the paper is computed from it; see archive/README.md for why it
# was abandoned (period of creation entered as an active variable makes the
# principal axis an axis of time). Install these only to re-run the diagnostics.
prince==0.16.5
scikit-learn==1.7.2
adjustText>=0.8
